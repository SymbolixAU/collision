# An object of class `dsmodel` fit using `Distance::ds` The example model is fit to the data `book.tee.data` from `mrds`

\#' @docType data

## Usage

``` r
ds_example
```

## Format

An object of class `dsmodel` of length 3.
