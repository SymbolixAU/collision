# rpm to rad_per_sec

Convert rotational speed in rpm to radians per second. ROT rpm x
(1/60sec) x (2pi rad) = ROT' rad / sec

## Usage

``` r
rpm_to_radpersec(rpm)
```

## Arguments

- rpm:

  rotor speed in RPM

## Examples

``` r
rpm_to_radpersec(1) # 0.1047
#> [1] 0.1047198
 
```
